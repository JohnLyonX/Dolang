# DOC-009: 第 17 章（测试与调试）是全书最薄弱章节

**优先级：** P2
**影响章节：** `docs/guide/17-testing-and-debugging.md`

## 问题描述

第 17 章目前只有 113 行，其中大部分是代码缩进块，实质文字极少。相比其他章节，这章明显是"占位"状态：

**缺失内容：**

1. **没有断言机制说明**：`std.core.check.assert` 在第 9 章一笔带过，第 17 章完全没有展示如何用它写验证逻辑。
2. **没有测试文件组织规范**：`.dol` 测试文件应该怎么写？放在哪里？命名规范？
3. **`dolang test` 输出格式未展示**：通过/失败时终端分别显示什么？
4. **没有说明如何测试非 HTTP 的纯脚本逻辑**（只讲了 `--route` 测试 HTTP handler）。
5. **没有"最小可复现示例"的具体操作步骤**（只说了"用 REPL 做最小复现"，但没有示范）。

## 期望改进

重写第 17 章，覆盖：

### 1. 断言与验证
```dol
$mod std.core.check;
check.assert(1 + 1 == 2, "基础算术");
check.assert("hello".len() == 5, "字符串长度");
```

### 2. 测试文件结构建议
```
my-app/
├── main.dol
├── tests/
│   ├── test_math.dol      // 纯函数测试
│   └── test_users.dol     // 业务逻辑测试
```

### 3. `dolang test` 的实际输出样例
展示成功和失败时终端分别打印什么内容。

### 4. 调试工作流
- `$>>ERR(...)` vs `$>>`：什么时候用哪个
- `dolang test --route` 的输出含义
- 从报错信息定位到源码行号的方法

### 5. 常见调试场景
- Handler 返回了空 body
- 模块函数没有被调用
- 类型注解报错排查
