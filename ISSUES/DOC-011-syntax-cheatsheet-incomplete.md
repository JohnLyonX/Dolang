# DOC-011: 语法速查表（附录 A）覆盖不足 30% 的语法关键字

**优先级：** P2
**影响章节：** `docs/guide/appendix/syntax-cheatsheet.md`

## 问题描述

附录 A 目前只有 46 行，仅覆盖：变量声明、常量声明、函数、控制流基础、模块导入、HTTP 路由基础。

**完全缺失的语法：**

| 缺失项 | 关键字 |
|--------|--------|
| 异常处理 | `$try / $catch / $throw` |
| 错误输出 | `$>>ERR(...)` |
| 标准输入 | `$<<LINE(...)` |
| 环境变量 | `$<<ENV(...)` |
| 文件读写 | `$<<FILE(...)` / `$>>FILE(...)` |
| 用户自定义类型 | `$Type` |
| 类型注解 | `$ x: Int = 0`、`-> List<User>` |
| JSON 构造器 | `$JSON { ... }` |
| Response 构造器 | `$RES(status, body)` |
| HTML 构造器 | `$HTML(...)` |
| Header 读取 | `$HDR("Name")` |
| 响应头注解 | `@SET_HDR({ ... })` |
| CORS 注解 | `@CORS(...)` |
| HTTP 路由块 | `$HTTP("/prefix") { ... }` |
| 路由模块链接 | `$HTTP(...).link("routers.api")` |
| 静态资源 | `$STATIC("/url", "dir")` |
| 程序入口 | `$main() {}` |
| 无限循环 | `$loop` |
| 计数循环 | `$for i = 0; i < n; i = i + 1` |
| 退出程序 | `exit` |

## 期望改进

将附录 A 扩展为完整的语法速查表，按功能分组，每组给出最小可识别的代码片段。目标是：用户在忘记某个关键字的写法时，能在附录 A 直接查到，而不用翻遍 18 章。

建议结构：
- 声明与赋值
- 输入与输出
- 控制流
- 函数
- 错误处理
- 模块
- 类型系统
- HTTP Handler
- HTTP 组织
- 构造器
- 注解
- 程序入口与退出
