# 注释功能测试结果

## 测试日期
2026-03-04

## 测试结果汇总

### 文件模式 (全部通过 ✅)

| 场景 | 结果 |
|------|------|
| 单行注释 - 整行 | ✅ |
| 单行注释 - 行尾 | ✅ |
| 单行注释 - 中文 | ✅ |
| 多行注释 - 单行 | ✅ |
| 多行注释 - 跨行 | ✅ |
| 未闭合多行注释 | ✅ (正确报错) |
| 多个单行注释 | ✅ |

### REPL 模式

#### 已修复 ✅

| 场景 | 结果 |
|------|------|
| 单行注释 - 整行 | ✅ |
| 单行注释 - 行尾 | ✅ |
| 单行注释 - 中文 | ✅ |
| 多行注释 - 单行 | ✅ |
| 多行注释跨行 (函数内) | ✅ |
| 注释在 `}` 前面 | ✅ |
| 多个单行注释 | ✅ |

#### 未修复 ❌

| 场景 | 结果 | 问题描述 |
|------|------|----------|
| 单行注释在函数内行首 | ❌ | `$fn test() { // 注释` 报错 `[ERROR] missing ';'` |
| 单行注释在语句后 | ❌ | `$ a = 1; // 注释` 在函数内报错 `[ERROR] invalid statement` |
| 未闭合多行注释 | ❌ | `/* 未闭合` 报错 `[ERROR] missing ';'` 而非 `unterminated multi-line comment` |

---

## 剩余 Bug 详细分析

### Bug 1: 单行注释在函数内行首

**测试命令**:
```bash
$fn test() {
    // 单行注释在函数内
    $ a = 1;
    $>> a;
}
```

**实际输出**:
```
.. .. .. .. [ERROR] missing ';'
[ERROR] runtime error: function 'test' is not defined
```

**问题原因**:
REPL 在收集多行输入时，使用大括号计数来确定何时结束输入。当遇到 `//` 时，`strip_comments` 函数会截断字符串，导致 `}` 被移除，从而无法正确计算嵌套深度。

### Bug 2: 单行注释在函数内语句后

**测试命令**:
```bash
$fn test() {
    $ a = 1; // 注释
    $>> a;
}
```

**实际输出**:
```
.. .. .. [ERROR] invalid statement
[ERROR] runtime error: function 'test' is not defined
```

**问题原因**:
当多行输入被合并后，单行注释 `//` 后的内容被截断，但解析器在处理时出现问题。

### Bug 3: 未闭合多行注释未正确报错

**测试命令**:
```bash
$ a = 1; /* 未闭合
```

**实际输出**:
```
[ERROR] missing ';'
```

**期望输出**:
```
[ERROR] lexer error: unterminated multi-line comment
```

**问题原因**:
`strip_comments` 函数没有正确处理未闭合的多行注释，只是简单地移除了开始标记，但没有检测到错误。

---

## 修复方向

需要在 `strip_comments` 函数中正确处理：

1. **大括号内的单行注释**：当在多行输入（函数体）内时，单行注释不应截断后续内容
2. **未闭合的多行注释**：应该返回错误而非静默处理
