# Dolang 是什么，以及我们为什么做它

> *"我们不关心工程优雅，我们关心最快把想法变成收入。"*

---

## 起源

Dolang 的诞生，多少带着一点玩的意思。

语言的核心符号是 `$` —— 这不是巧合。`$` 是全世界最通用的财富符号，美金，财富，钱。我们用它来声明变量、声明常量、打印输出，把每一行代码都和「赚钱」这件事绑定在一起。

**写代码，就是赚钱。这是 Dolang 最初的态度。**

---

## 我们在解决什么问题

PHP 诞生于一个时代：「我要快速搭一个网页」。

那个时代的问题已经被解决了。但时代之后，出现了新的问题，还没有人真正解决：

- 我不想学框架，我只想**快速验证一个 API 想法**
- 我想把一个脚本**直接暴露成可调用的服务**
- 我不在乎性能，我在乎的是**从想法到跑起来的时间**
- 我想要**今天就能开张**，而不是明天

这就是 Dolang 想卡的位置。

---

## 我们和 PHP 不一样在哪里

这个问题值得认真回答，因为表面上我们都在做「快速 Web 服务」。

| | PHP | Dolang |
|---|---|---|
| 定位 | 建房子 | 摆摊 |
| 目标 | 工程构建 | 变现验证 |
| 学习成本 | 需要框架、路由、模板 | 6 个符号，5 分钟上手 |
| 时代背景 | 30 年前的 Web | 独立开发者的今天 |
| 态度 | 严肃的工程语言 | 反严肃，追求最快落地 |

**PHP 是建房子的，Dolang 是摆摊的。**

摆摊不需要地基，不需要设计图纸，需要的是今天就能开张，明天就能收钱。

---

## 我们的用户是谁

Dolang 面向**独立开发者**——那些同时身兼产品经理、设计师、工程师、运营的人。

他们的时间是稀缺的，他们的耐心是有限的，他们最不需要的就是在「从想法到上线」这条路上多走任何一步弯路。

Dolang 的设计哲学，从第一个符号开始，就为这类人服务：

- `$` 声明变量 —— 简单，直接
- `$@` 声明常量 —— 保护重要的东西
- `$>>` 打印输出 —— 立刻看到结果
- 整套语法 6 个符号 —— 今天学，今天用

---

## 未来的方向

Dolang 目前是一个解释型脚本语言的起点。

我们下一步想做的，是让「变现」这个动作成为语言的一等公民——让支付、Webhook、API 暴露不再是靠第三方库拼凑出来的，而是内建在语言的表达能力里。

我们想要的终态是：

> 一个 `.dao` 文件，一条命令，直接是一个能收钱的服务。

这个目标听起来简单，但它需要的不是更复杂的语言，而是更极致的取舍。

---

## 一句话总结

**Dolang 是面向独立开发者的快速变现脚本语言。**

`$` 不只是变量符号，它是我们对这门语言全部野心的浓缩：

写代码，就是赚钱。

---

*Dolang Team · v1.2 · MIT License*

---

# Dolang 官方文档

<p align="center">
  <img src="https://img.shields.io/badge/version-v1.2-blue" alt="Version">
  <img src="https://img.shields.io/badge/Rust-1.70+-orange" alt="Rust">
  <img src="https://img.shields.io/badge/License-MIT-green" alt="License">
</p>

Dolang 是一个用 Rust 编写的轻量级解释型编程语言，采用独特的**符号化语法**设计。

## 目录

1. [快速开始](#快速开始)
2. [基础语法](#基础语法)
3. [关键字](#关键字)
4. [数据类型](#数据类型)
5. [变量与常量](#变量与常量)
6. [类型系统](#类型系统)
7. [表达式与运算](#表达式与运算)
8. [语句](#语句)
9. [错误处理](#错误处理)
10. [语言特色](#语言特色)
11. [核心优势](#核心优势)
12. [运行方式](#运行方式)
13. [开发计划](#开发计划)

---

## 快速开始

### 安装

```bash
# 克隆项目
git clone https://github.com/your-repo/daolang.git
cd daolang

# 编译
cargo build

# 运行 REPL
cargo run
```

### 你的第一个程序

```
Dolang REPL v1.2
>> $>> "Hello, Dolang!";
Hello, Dolang!
>>
```

### 退出 REPL

输入以下命令退出：
- `exit`
- `exit()`
- `quit`

---

## 基础语法

### 语句结束符

所有语句必须以 `;` 结尾：

```dao
$>> 1;
1

$>> "Hello";
Hello
```

### 注释

Dolang 支持两种注释语法：

#### 单行注释

使用 `//` 开头到行尾：

```dao
$ a = 1; // 这是一个注释
$>> a;   // 输出: 1
```

#### 多行注释

使用 `/* ... */` 包裹：

```dao
$ a = 1; /* 这是一个
多行注释 */ $>> a;
```

---

## 关键字

Dolang 使用特定符号和关键字来构建语法。以下是所有关键字的说明：

### 声明关键字

| 关键字 | 说明 | 示例 |
|--------|------|------|
| `$` | 变量声明 | `$ a = 1;` |
| `$@` | 常量声明 | `$@ PI = 3.14;` |
| `$fn` | 函数声明 | `$fn add(a, b) { ... }` |

### 输出关键字

| 关键字 | 说明 | 示例 |
|--------|------|------|
| `$>>` | 打印输出（终端） | `$>> "Hello";` |
| `$#` | 函数返回值 | `$# value;` |

### 流程控制关键字

| 关键字 | 说明 | 示例 |
|--------|------|------|
| `$if` | 条件判断 | `$if condition { ... }` |
| `$elif` | 条件分支 | `$elif condition { ... }` |
| `$else` | 默认分支 | `$else { ... }` |
| `$while` | 当循环 | `$while condition { ... }` |
| `$loop` | 无限循环 | `$loop { ... }` |
| `$for` | 计数循环 | `$for i = 0; i < 10; i = i + 1 { ... }` |
| `$break` | 跳出循环 | `$break;` |
| `$continue` | 继续下次循环 | `$continue;` |

### 运算符

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `=` | 赋值 | `a = 10;` |
| `+` | 加法 | `1 + 2` |
| `-` | 减法 | `5 - 3` |
| `*` | 乘法 | `4 * 3` |
| `/` | 除法（浮点） | `10 / 3` → `3.33...` |
| `%` | 取模 | `10 % 3` |
| `==` | 相等 | `a == b` |
| `!=` | 不等 | `a != b` |
| `>` | 大于 | `a > b` |
| `<` | 小于 | `a < b` |
| `>=` | 大于等于 | `a >= b` |
| `<=` | 小于等于 | `a <= b` |
| `&&` | 逻辑与 | `a && b` |
| `\|\|` | 逻辑或 | `a \|\| b` |
| `!` | 逻辑非 | `!a` |

### 代码块

| 符号 | 说明 | 示例 |
|------|------|------|
| `{` | 代码块开始 | `$if ... { ... }` |
| `}` | 代码块结束 | `$if ... { ... }` |
| `;` | 语句结束 | `$>> a;` |

### 字面量

| 字面量 | 说明 | 示例 |
|--------|------|------|
| 整数 | 正负整数 | `42`, `-17`, `0` |
| 浮点数 | 小数 | `3.14`, `-2.5` |
| 字符串 | 双引号包裹 | `"Hello"` |
| 字符 | 单引号包裹 | `'a'` |
| 布尔 | 真/假 | `true`, `false` |
| 列表 | 有序集合 | `[1, 2, 3]`, `[]` |
| 字典 | 键值对集合 | `{"key": "value"}`, `{}` |

---

## 数据类型

Dolang 是动态类型语言，支持以下数据类型：

### 整数 (Integer)

表示整数值，支持正数和负数：

```dao
$>> 42;
42

$>> -17;
-17
```

**数值范围**：取决于实现，通常为 64 位有符号整数

---

### 浮点数 (Float)

表示小数数值：

```dao
$>> 3.14;
3.14

$>> -2.5;
-2.5

$>> 0.5;
0.5
```

---

### 字符串 (String)

使用双引号 `"..."` 包裹的文本：

```dao
$>> "Hello World";
Hello World

$>> "";
(空输出)

$>> "Dao" + "Lang";
Dolang
```

**特性**：
- 支持 `+` 运算符进行字符串连接
- 内部统一存储为字符串类型

---

### 字符 (Char)

使用单引号 `'...'` 包裹的单个字符：

```dao
$>> 'a';
a

$>> 'Z';
Z
```

**注意**：字符类型在内部也以字符串形式存储

---

### 布尔 (Boolean)

表示真值，`true` 或 `false`：

```dao
$>> true;
true

$>> false;
false
```

**注意**：布尔类型主要用于条件判断

---

### 列表 (List)

表示一组有序的值集合：

```dao
$ arr = [1, 2, 3];
$>> arr[0];
1

$>> arr[2];
3
```

**空列表**：

```dao
$ empty = [];
$>> empty;
[]
```

**混合类型**：

```dao
$ mixed = [1, "hello", true];
$>> mixed[0];
1

$>> mixed[1];
hello

$>> mixed[2];
true
```

**嵌套列表**：

```dao
$ matrix = [[1, 2], [3, 4]];
$>> matrix[0][0];
1

$>> matrix[1][1];
4
```

**修改列表元素**：

```dao
$ arr = [1, 2, 3];
arr[1] = 99;
$>> arr[1];
99
```

---

### 字典 (Map)

表示键值对集合：

```dao
$ user = {"name": "Tom", "age": 18};
$>> user["name"];
Tom

$>> user["age"];
18
```

**空字典**：

```dao
$ empty = {};
$>> empty;
{}
```

**修改字典值**：

```dao
$ user = {"name": "Tom"};
user["email"] = "tom@example.com";
$>> user["email"];
tom@example.com
```

**嵌套字典**：

```dao
$ config = {"db": {"host": "localhost", "port": 5432}};
$>> config["db"]["host"];
localhost

$>> config["db"]["port"];
5432
```

**字典与列表混用**：

```dao
$ data = {"tags": [1, 2, 3], "name": "test"};
$>> data["tags"][0];
1

$>> data["name"];
test
```

---

### 方法调用 (Method Calls)

使用点语法调用对象的方法：

```dao
$ arr = [1, 2, 3];
$>> arr.len();
3

$ s = "Hello";
$>> s.upper();
HELLO
```

**链式调用**：

```dao
$ arr = [1, 2, 3];
$>> arr.push(4).push(5).len();
5
```

---

### 内置方法

#### 字符串方法

```dao
$ s = "Hello, World";
$>> s.len();              // 字符串长度
12

$>> s.upper();           // 转大写
HELLO, WORLD

$>> s.lower();           // 转小写
hello, world

$>> s.contains("World"); // 判断包含
true

$>> s.replace("World", "Dolang");  // 替换
Hello, Dolang

$>> "a,b,c".split(",");  // 分割成列表
[a, b, c]

$>> "hello".slice(1, 4); // 截取子串
ell
```

#### 列表方法

```dao
$ arr = [3, 1, 2];
$>> arr.len();           // 元素数量
3

$>> arr.contains(1);    // 判断包含
true

$>> arr.push(4);        // 末尾添加
[3, 1, 2, 4]

$>> arr.reverse();      // 反转
[2, 1, 3]

$>> arr.join(", ");     // 拼接为字符串
3, 1, 2
```

#### 字典方法

```dao
$ user = {"name": "Tom", "age": 18};
$>> user.len();         // 键值对数量
2

$>> user.keys();        // 获取所有键
[name, age]

$>> user.values();      // 获取所有值
[Tom, 18]

$>> user.contains_key("name");  // 判断键是否存在
true
```

---

### for-in 遍历

遍历列表、字典或字符串：

```dao
// 遍历列表
$ arr = [1, 2, 3];
$for item in arr {
    $>> item;
}
// 输出: 1, 2, 3

// 遍历字典 (遍历键)
$ user = {"name": "Tom", "age": 18};
$for key in user {
    $>> key;
}
// 输出: name, age

// 遍历字符串 (遍历字符)
$ s = "abc";
$for ch in s {
    $>> ch;
}
// 输出: a, b, c
```

---

## 变量与常量

### 变量声明

使用 `$` 前缀声明变量：

```dao
$ a = 1;
$>> a;
1
```

语法：`$ 变量名 = 值;`

### 变量赋值

不使用 `$` 前缀时，为重新赋值：

```dao
$ a = 1;
a = 2;
$>> a;
2
```

### 常量声明

使用 `$@` 前缀声明常量：

```dao
$@ PI = 3.14;
$>> PI;
3.14
```

常量不可修改：

```dao
$@ PI = 3.14;
PI = 3.14159;
```

运行结果：
```
[ERROR] runtime error: cannot reassign constant 'PI'
```

### 变量遮蔽 (Shadowing)

使用 `$` 前缀可以声明同名变量（遮蔽），允许不同类型：

```dao
$ a = 20;
$>> a;
20

$ a = "hello";
$>> a;
hello
```

### 变量读取

使用变量名直接读取变量值：

```dao
$ name = "Dolang";
$>> name;
Dolang
```

**使用技巧**：

- 变量在使用前必须先声明，否则会报错
- 变量名区分大小写：`$a` 和 `$A` 是不同的变量
- 建议使用有意义的变量名，如 `$count`、`$name` 等

---

## 类型系统

### 自动类型推断

Dolang 是动态类型语言，无需显式声明类型：

```dao
$ a = 1;           // 自动识别为整数
$ b = 3.14;        // 自动识别为浮点数
$ c = "hello";      // 自动识别为字符串
$ d = 'x';         // 自动识别为字符
```

### 类型检查

重新赋值时必须保持类型一致：

```dao
$ a = 1;
a = 2;             // OK: 同为整数
a = 3.14;          // ERROR: 类型不匹配
```

### 变量遮蔽 (Shadowing)

使用 `$` 前缀可以声明同名变量（遮蔽），允许改变类型：

```dao
$ a = 20;           // a = 20 (整数)
$ a = "hello";       // 遮蔽，新 a = "hello" (字符串)
$>> a;                // 输出: hello
```

### 统一的数据表示

所有数据在内部统一存储为字符串，简化实现：

```dao
$>> 42;        // 输出: 42
$>> "hello";   // 输出: hello
$>> 'a';       // 输出: a
```

### 运行时类型获取

使用 `` 可以获取变量的当前类型信息（未来特性）：

```dao
$ a = 1;
$>> typeof(a);  // 输出: Integer
```

---

## 表达式与运算

Dolang 支持丰富的运算符，可以用于构建表达式。

### 算术运算

```dao
$>> 1 + 2;
3

$>> 10 - 3;
7

$>> 4 * 5;
20

$>> 10 / 3;
3.3333333333333335

$>> 10 % 3;
1

$>> "Hello" + " " + "World";
Hello World
```

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `+` | 加法 / 字符串连接 | `1 + 2` → `3` |
| `-` | 减法 | `5 - 3` → `2` |
| `*` | 乘法 | `4 * 3` → `12` |
| `/` | 除法（浮点） | `10 / 3` → `3.33...` |
| `%` | 取模 | `10 % 3` → `1` |

---

### 比较运算

```dao
$>> 5 == 5;
true

$>> 5 != 3;
true

$>> 10 > 5;
true

$>> 3 < 7;
true

$>> 5 >= 5;
true

$>> 3 <= 4;
true
```

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `==` | 相等 | `5 == 5` → `true` |
| `!=` | 不等 | `5 != 3` → `true` |
| `>` | 大于 | `10 > 5` → `true` |
| `<` | 小于 | `3 < 7` → `true` |
| `>=` | 大于等于 | `5 >= 5` → `true` |
| `<=` | 小于等于 | `3 <= 4` → `true` |

---

### 逻辑运算

```dao
$>> true && true;
true

$>> true && false;
false

$>> true || false;
true

$>> false || false;
false

$>> !false;
true

$>> !true;
false
```

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `&&` | 逻辑与 | `true && false` → `false` |
| `\|\|` | 逻辑或 | `false \|\| true` → `true` |
| `!` | 逻辑非 | `!true` → `false` |

---

## 语句

### 打印语句

使用 `$>>` 输出值：

```dao
$>> "Hello";
Hello

$ a = 42;
$>> a;
42

$>> 1 + 2 + 3;
123
```

### 赋值语句

变量赋值：

```dao
$ x = 10;
x = 20;
```

常量赋值：

```dao
$@ MAX = 100;
```

---

### 条件语句

使用 `$if`、`$elif`、`$else` 进行条件判断：

```dao
$ age = 18;

$if age >= 18 {
    $>> "adult";
} $elif age >= 12 {
    $>> "teenager";
} $else {
    $>> "child";
}
```

**语法**：

```dao
$if condition {
    statements;
} $elif condition {
    statements;
} $else {
    statements;
}
```

---

### 循环语句

#### $while 循环

当条件为真时重复执行：

```dao
$ count = 0;

$while count < 5 {
    $>> count;
    count = count + 1;
}
```

#### $loop 循环

无限循环，配合 `$break` 使用：

```dao
$ i = 0;

$loop {
    $>> i;
    i = i + 1;
    $if i >= 5 {
        $break;
    }
}
```

#### $for 循环

传统 for 循环语法：

```dao
$for i = 0; i < 5; i = i + 1 {
    $>> i;
}
```

**语法**：

```dao
$for init; condition; update {
    statements;
}
```

---

### 跳出语句

#### $break

跳出循环：

```dao
$ i = 0;

$loop {
    i = i + 1;
    $if i == 3 {
        $break;
    }
    $>> i;
}
```

#### $continue

跳过本次循环，继续下一次：

```dao
$for i = 0; i < 5; i = i + 1 {
    $if i == 2 {
        $continue;
    }
    $>> i;
}
```

---

### 退出语句

使用 `exit` 退出程序：

```dao
exit;
```

---

### 函数

使用 `$fn` 声明函数，`$#` 返回值。

#### 函数定义

```dao
$fn add(a, b) {
    $ result = a + b;
    $# result;
}
```

**语法**：

```dao
$fn 函数名(参数1, 参数2, ...) {
    函数体;
    $# 返回值;
}
```

**使用技巧**：

- 函数名在全局唯一，不能重复定义
- 参数是函数的局部变量，外部无法直接访问
- `$#` 语句会立即返回，后面的代码不会执行

#### 函数参数传递

参数按值传递，函数内部修改参数不会影响外部变量：

```dao
$fn increment(x) {
    x = x + 1;
    $>> x;
}

$ a = 10;
increment(a);    // 输出: 11
$>> a;           // 输出: 10 (a 未改变)
```

#### 无返回值函数

```dao
$fn greet(name) {
    $>> "Hello, " + name;
}

greet("Dolang");
```

#### 函数调用

```dao
$fn add(a, b) {
    $# a + b;
}

$ sum = add(1, 2);
$>> sum;
3
```

#### 函数作为值（匿名函数）

Dolang 支持将函数赋值给变量，实现函数作为一等公民（first-class functions）：

```dao
$a = 20;
$b = 30;

$result = $fn(x,y) {
 $>> x + y;
};

result(a,b);
50
```

**语法**：

```dao
$变量名 = $fn(参数1, 参数2, ...) {
    函数体;
};
```

- 匿名函数不需要函数名
- 函数体末尾需要分号结束
- 可以像调用普通函数一样调用通过变量持有的函数

**带返回值的匿名函数**：

```dao
$result = $fn(x, y) -> Int {
    $# x + y;
};

$>> result(10, 20);
30
```

**无返回值的匿名函数**：

```dao
$result = $fn(x, y) {
    $>> x + y;
};

result(10, 20);
30
```

#### 返回语句

使用 `$#` 返回值：

```dao
$fn double(x) {
    $# x * 2;
}

$>> double(5);
10
```

#### 返回类型声明

可以使用 `-> Type` 指定函数返回类型：

```dao
$fn double(x) -> Int {
    $# x * 2;
}

$>> double(5);
10
```

支持的类型：
- `Int` / `Integer` - 整数类型
- `Float` - 浮点数类型
- `String` - 字符串类型
- `Bool` / `Boolean` - 布尔类型

**返回类型不匹配时报错**：

```dao
$fn test() -> String {
    $# 123;
}
```

运行结果：
```
[ERROR] runtime error: function 'test' expects return type 'String' but got 'Int'
```

**函数有返回值但未声明返回类型时报错**：

```dao
$fn add(x, y) {
    $# x + y;
}
```

运行结果：
```
[ERROR] runtime error: function 'add' has no return type declared but returns a value
```

**无返回值的函数（void 函数）**：

```dao
$fn greet(name) {
    $>> "Hello, " + name;
}
greet("World");
```

运行结果：
```
Hello, World
```

#### 递归支持

Dolang 支持递归函数：

```dao
$fn factorial(n) {
    $if n <= 1 {
        $# 1;
    } $else {
        $# n * factorial(n - 1);
    }
}

$>> factorial(5);
120
```

#### 作用域规则

函数内部有自己的作用域，外部变量无法直接访问：

```dao
$ x = 10;

$fn test() {
    $>> x;  // ERROR: variable 'x' not found
}

test();
```

要访问外部变量，需要通过参数传递：

```dao
$fn print_val(x) {
    $>> x;
}

$ x = 10;
print_val(x);  // 输出: 10
```

---

## 错误处理

### 语法错误

Dolang 提供详细的解析错误信息，包括错误位置、找到的 token 和期望的内容：

```dao
$result = $fn(x,y) {
 x + y
}

result(a,b);
```

运行结果：
```
[ERROR] Parse error at line 1:1
  found: Ident
  unexpected token after expression
```

**错误信息包含**：
- 错误位置（行:列）
- 找到的 token 类型
- 期望的内容或错误描述

### 类型错误

```dao
$ a = 1;
a = "hello";
```

运行结果：
```
[ERROR] runtime error: type mismatch: cannot assign hello to variable 'a' of type Number
```

### 常量重赋值错误

```dao
$@ CONST = 100;
CONST = 200;
```

运行结果：
```
[ERROR] runtime error: cannot reassign constant 'CONST'
```

### 函数调用错误

#### 调用未定义的函数

```dao
foo();
```

运行结果：
```
[ERROR] runtime error: function 'foo' is not defined
```

#### 函数重定义错误

```dao
$fn add(a, b) {
    $# a + b;
}
$fn add(a, b) {
    $# a;
}
```

运行结果：
```
[ERROR] runtime error: function 'add' is already defined
```

---

## 语言特色

### 完善的 REPL 交互体验

提供功能完备的交互式解释器：

- **历史记录**：上下方向键遍历历史命令
- **光标编辑**：左右方向键移动光标
- **快捷键**：Ctrl+A 跳转行首，Ctrl+E 跳转行末
- **中断处理**：Ctrl+C 取消当前行，Ctrl+D 退出程序

### 多行输入支持

REPL 支持多行输入，当输入未闭合的括号时会自动继续读取：

```dao
>> $fn fib(n) {
>>     $if n <= 1 {
>>         $# n;
>>     } $else {
>>         $# fib(n - 1) + fib(n - 2);
>>     }
>> }
```

输入 `}` 闭合代码块后按回车执行。

### 实用技巧

#### 1. 快速计算

```dao
>> $>> 1 + 2 * 3;
7

>> $>> (1 + 2) * 3;
9
```

#### 2. 字符串处理

```dao
>> $ name = "Dolang";
>> $>> "Hello, " + name + "!";
Hello, Dolang!
```

#### 3. 条件表达式

```dao
>> $ x = 10;
>> $if x > 5 {
>>     $>> "big";
>> } $else {
>>     $>> "small";
>> }
big
```

#### 4. 循环计算

```dao
>> $ sum = 0;
>> $while sum < 100 {
>>     sum = sum + 1;
>> }
>> $>> sum;
100
```

#### 5. 使用函数封装逻辑

```dao
>> $fn factorial(n) {
>>     $if n <= 1 {
>>         $# 1;
>>     } $else {
>>         $# n * factorial(n - 1);
>>     }
>> }
>> $>> factorial(5);
120
```

---

## 核心优势

### 1. 极简语法设计

Dolang 追求极致的简洁，核心语法符号：

| 语法 | 功能 |
|------|------|
| `$` | 变量声明 |
| `$@` | 常量声明 |
| `` | 变量读取 |
| `$>>` | 打印输出 |
| `=` | 赋值操作 |
| `+`, `-`, `*`, `/`, `%` | 算术运算 |
| `==`, `!=`, `>`, `<`, `>=`, `<=` | 比较运算 |
| `&&`, `\|\|`, `!` | 逻辑运算 |
| `$if`, `$elif`, `$else` | 条件语句 |
| `$while`, `$loop`, `$for` | 循环语句 |
| `$break`, `$continue` | 跳出语句 |

**优势**：学习成本极低，上手即用，用户可以在 5 分钟内掌握全部语法。

### 2. 独特的符号化语法

使用 `$`、`$@`、`$>>` 等符号构建语句，形成鲜明的视觉风格：

```dao
$ a = 1;        // 声明变量
$@ PI = 3.14;   // 声明常量
$>> a;           // 打印输出
```

### 3. 强大的变量系统

#### 变量遮蔽 (Shadowing)

允许使用 `$` 符号重新声明同名变量，实现变量遮蔽：

```dao
$ a = 20;           // a = 20 (数字)
$ a = "hello";      // 遮蔽，新 a = "hello" (字符串)
$>> a;               // 输出: hello
```

**优势**：提供灵活的变量重绑定能力，适合需要临时变量的场景。

#### 类型安全

不使用 `$` 前缀时，赋值必须保持类型一致：

```dao
$ a = 20;
a = 30;             // OK: 类型一致
a = "hello";        // ERROR: type mismatch
```

**优势**：在重新赋值时提供类型保护，防止意外的类型转换。

### 4. 轻量级实现

核心实现采用模块化设计，代码结构清晰：

```
src/
├── main.rs              # 程序入口
├── cli.rs               # CLI 参数处理
├── repl.rs              # REPL 交互逻辑
├── token.rs             # 词法单元定义
├── lexer.rs             # 词法分析器
├── parser/              # 语法解析器模块
│   ├── mod.rs           # 统一导出
│   ├── expr.rs          # 表达式解析
│   ├── stmt.rs          # 语句解析
│   └── literal.rs       # 字面量解析
├── ast.rs               # 抽象语法树
├── interpreter/          # 解释执行模块
│   ├── mod.rs           # 统一导出
│   ├── env.rs           # 环境类型管理
│   ├── eval.rs          # 表达式求值
│   └── exec.rs          # 语句执行
├── error.rs             # 错误类型定义
└── syntax.rs            # 语法验证
```

**优势**：
- 跨平台支持（使用 `crossterm`，支持 Linux/macOS/Windows）
- 模块化设计，易于维护和扩展
- 编译快速
- 易于学习和二次开发

### 5. Rust 驱动的高性能

采用 Rust 语言实现，带来以下优势：

- **内存安全**：无 GC，无悬挂指针
- **高性能**：接近原生的执行效率
- **跨平台**：支持 Linux、macOS 等 Unix 系统

---

## 与传统语言的对比

| 特性 | Dolang | 传统语言 (JavaScript/Python) |
|------|---------|------------------------------|
| 变量声明 | `$ var = value;` | `let var = value;` |
| 常量声明 | `$@ var = value;` | `const var = value;` |
| 打印 | `$>> expr;` | `print(expr)` / `console.log(expr)` |
| 赋值 | `=` | `=` |
| 变量读取 | `var` 或直接写变量名 | 直接写变量名 |

---

## 运行方式

### 编译与运行

```bash
# 编译项目
cargo build

# 运行 REPL
cargo run

# 运行测试
cargo test
```

### 直接运行二进制

```bash
./target/debug/daolang
```

### REPL 使用

- 输入代码后按回车执行
- 使用上下方向键查看历史
- 使用左右方向键移动光标
- 使用 Ctrl+C 中断输入
- 输入 `exit` 或 `quit` 退出

---

## 开发计划

### 当前版本

- **v1.2**: 代码解耦、模块化重构、复合赋值运算符、列表类型、字典类型、方法调用、for-in遍历

### 已完成功能 (v1.0)

- [x] 整数类型
- [x] 浮点数类型
- [x] 字符串类型
- [x] 字符类型
- [x] 布尔类型
- [x] 列表类型 (List)
- [x] 字典类型 (Map)
- [x] 方法调用 (obj.method())
- [x] 字符串内置方法 (len, upper, lower, contains, etc.)
- [x] 列表内置方法 (push, pop, reverse, etc.)
- [x] 字典内置方法 (keys, values, etc.)
- [x] for-in 遍历语法
- [x] 变量声明 (`$`)
- [x] 常量声明 (`$@`)
- [x] 变量遮蔽 (Shadowing)
- [x] 类型检查
- [x] 算术运算 (`+`, `-`, `*`, `/`, `%`)
- [x] 复合赋值 (`+=`, `-=`, `*=`, `/=`, `%=`)
- [x] 比较运算 (`==`, `!=`, `>`, `<`, `>=`, `<=`)
- [x] 逻辑运算 (`&&`, `||`, `!`)
- [x] 条件语句 (`$if`, `$elif`, `$else`)
- [x] 循环语句 (`$while`, `$loop`, `$for`)
- [x] 跳出语句 (`$break`, `$continue`)
- [x] 退出语句 (`exit`)
- [x] 注释 (`//`, `/* */`)
- [x] 函数定义 (`$fn`)
- [x] 函数调用
- [x] 返回语句 (`$#`)
- [x] 递归支持
- [x] 作用域隔离
- [x] 函数调用错误检测 (调用未定义函数)
- [x] 函数重定义错误检测
- [x] 函数返回类型声明与检查

### 待实现功能

#### 中优先级

- 块语句 `{ }`
- 字符串方法

#### 低优先级
- 模块系统

---

## 常见问题 (FAQ)

### Q1: Dolang 和其他脚本语言有什么区别？

Dolang 的核心特点是**符号化语法**和**快速验证**。使用 `$` 作为变量声明前缀，`$>>` 作为打印输出，形成独特的视觉风格。适合需要快速原型验证的场景。

### Q2: 如何退出 REPL？

输入以下任一命令：
- `exit`
- `exit()`
- `quit`

或使用快捷键 Ctrl+D（当输入为空时）。

### Q3: 为什么变量赋值需要区分 `$` 和 `=`？

- `$ name = value`：声明新变量（或遮蔽已有变量）
- `name = value`：重新赋值已有变量，必须保持类型一致

这设计是为了：
- 防止意外创建新变量（拼写错误）
- 提供类型安全保护

### Q4: 函数可以重定义吗？

不可以。重复定义同名函数会报错：

```
[ERROR] runtime error: function 'add' is already defined
```

如需重新定义，请先退出 REPL 重新开始。

### Q5: 如何处理除零错误？

除法和取模运算除以零时不会崩溃，而是返回错误：

```dao
>> $>> 10 / 0;
[ERROR] runtime error: division by zero
```

### Q6: 支持递归吗？

支持。Dolang 支持函数递归调用：

```dao
$fn fib(n) {
    $if n <= 1 {
        $# n;
    } $else {
        $# fib(n - 1) + fib(n - 2);
    }
}
$>> fib(10);
55
```

### Q7: 匿名函数和普通函数有什么区别？

匿名函数可以赋值给变量，作为一等公民使用：

```dao
$ add = $fn(a, b) { $# a + b; };
$>> add(1, 2);
3
```

普通函数是全局定义的，不能赋值给变量。

### Q8: 如何调试程序？

使用 `$>>` 打印变量值：

```dao
$ x = 10;
$>> "x = " + x;
x = 10
```

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.2 | 2026-02-28 | 代码解耦、复合赋值运算符、列表类型、字典类型、方法调用、for-in遍历 |
| v1.2 | 2026-02-27 | 代码解耦：拆分 parser、interpreter、main 模块，提升代码可维护性 |
| v1.1 | 2026-02-27 | 添加函数作为值（匿名函数赋值给变量）、改进解析错误消息显示 |
| v1.0 | 2026-02-27 | 添加函数系统 ($fn)、$# 返回语句、函数返回类型检查、未定义变量检查 |
| v0.9 | 2026-02-27 | 添加循环语句 ($while/$loop/$for) |
| v0.8 | 2026-02-27 | 添加条件语句 ($if/$elif/$else) |
| v0.7 | 2026-02-27 | 添加布尔类型、比较/逻辑运算 |
| v0.6 | 2026-02-26 | 添加变量遮蔽、类型系统 |
| v0.5 | 2026-02-26 | 初始版本，重构语法 |
| v0.1 | - | 原始版本 (..$ 语法) |

---

<p align="center">
  © 2026 Dolang Team. MIT License.
</p>
