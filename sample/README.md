# Sample Projects

这个目录用于存放真实 Dolang 项目级测试样例。

当前首个计划中的样例项目：

- `sample/auth-b2b-portal`

用途：

- 验证 `serve` 模式下的真实项目结构
- 验证 `std.auth.session`、`std.auth.jwt`、`std.auth.guard`、`std.auth.csrf`
- 验证 cookie session、JWT bearer、refresh token、Postgres session store
- 作为后续回归测试和文档示例的项目级基线
